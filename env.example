# Environment Variables Configuration
# Copy this file to .env and update with your values

# ============================================
# Server Configuration
# ============================================
# PORT - Server port (default: 5000 for local, auto-set by platforms for deployment)
# Local development uses 5000
# Render uses $PORT (auto-assigned)
# Railway uses $PORT (auto-assigned)
# Cloud Run uses PORT env var (typically 8080)
# Fly.io uses internal_port from fly.toml (8080)
PORT=5000

# Environment
# Set to 'production' for deployed environments
FLASK_ENV=development

# Secret Key (generate a strong secret for production)
SECRET_KEY=your-secret-key-here

# ============================================
# Database Configuration
# ============================================
# For local PostgreSQL
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=distress_detection
# DB_USER=postgres
# DB_PASSWORD=admin

# For Render PostgreSQL (already configured in render.yaml)
DB_HOST=dpg-d3su87c9c44c73cdki1g-a.oregon-postgres.render.com
DB_PORT=5432
DB_NAME=distress_db
DB_USER=distress_db_user
DB_PASSWORD=xZLgwESqa0z9OeMtZiAlYbidFQqLHDMv

# ============================================
# Application Settings
# ============================================
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=16777216
WEB_CONCURRENCY=1
